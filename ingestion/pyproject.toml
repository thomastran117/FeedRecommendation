[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "thing-ingestion"
version = "0.1.0"
description = "RSS discovery and article scraping job for Thing."
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4>=4.12.3,<5",
    "psycopg[binary]>=3.2.9,<4",
    "PyYAML>=6.0.2,<7",
    "requests>=2.32.3,<3",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3.5,<9",
]

[tool.pytest.ini_options]
pythonpath = [
    "src",
]
testpaths = [
    "tests",
]
