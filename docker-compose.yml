version: "3.9"

services:
  postgres:
    image: postgres:16
    container_name: app-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: appdb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d appdb"]
      interval: 10s
      timeout: 5s
      retries: 5

  client:
    build:
      context: ./client
    container_name: app-client
    restart: unless-stopped
    depends_on:
      - server
    ports:
      - "80:80"

  server:
    build:
      context: ./server
    container_name: app-server
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql://postgres:postgres@postgres:5432/appdb}
      REDIS_URL: ${REDIS_URL:-redis://redis:6379}
      RABBITMQ_URL: ${RABBITMQ_URL:-amqp://guest:guest@rabbitmq:5672/}
      SECRET_KEY: ${SECRET_KEY:-change-me-in-production}
    ports:
      - "8000:8000"

  ingestion:
    build:
      context: ./ingestion
    container_name: app-ingestion
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      THING_DATABASE_URL: ${THING_DATABASE_URL:-postgresql://postgres:postgres@postgres:5432/appdb}
      THING_DATABASE_HOST: ${THING_DATABASE_HOST:-postgres}
      THING_DATABASE_PORT: ${THING_DATABASE_PORT:-5432}
      THING_INGESTION_SCHEDULE: "${THING_INGESTION_SCHEDULE:-*/15 * * * *}"

  feed:
    build:
      context: ./feed
    container_name: app-feed
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      THING_DATABASE_URL: ${THING_DATABASE_URL:-postgresql://postgres:postgres@postgres:5432/appdb}
      THING_DATABASE_HOST: ${THING_DATABASE_HOST:-postgres}
      THING_DATABASE_PORT: ${THING_DATABASE_PORT:-5432}
      THING_FEED_SCHEDULE: "${THING_FEED_SCHEDULE:-*/15 * * * *}"

  redis:
    image: redis:7
    container_name: app-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: ["redis-server", "--appendonly", "yes"]
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  rabbitmq:
    image: rabbitmq:3-management
    container_name: app-rabbitmq
    restart: unless-stopped
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    ports:
      - "5672:5672"
      - "15672:15672"
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 15s
      timeout: 10s
      retries: 5

volumes:
  postgres_data:
  redis_data:
  rabbitmq_data:
